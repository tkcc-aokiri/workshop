# Pomodoro Timer 段階的実装計画

このドキュメントは、architecture.md と features.md を踏まえた段階的な実装計画を定義する。

## 実装方針（粒度）

- 1フェーズ = 1つの主要責務に集中する
- 1フェーズの完了条件 = 画面確認 + 最低1つのテスト通過
- 1PRの目安 = 200〜500行、3〜6ファイル程度
- UI調整とロジック変更を同一PRに混ぜすぎない

## フェーズ計画

### フェーズ0: 土台作成（半日）

目的:
- 実装を進められる最小構成を作る

実装内容:
- Flask起動と `GET /` で画面配信
- ディレクトリひな形作成（templates, static/css, static/js, tests）

完了条件:
- ブラウザでトップ画面が表示される
- 最小のスモークテストが通る

### フェーズ1: タイマー表示のみ（1日）

目的:
- UIと時間表示の接続確認

実装内容:
- 固定値で残り時間表示
- フロントの描画更新ループ（表示更新のみ）

完了条件:
- 秒表示が崩れず更新される
- UIモックに近い基本レイアウトができる

### フェーズ2: timer-engine導入（1日）

目的:
- 時間計算をUIから分離する

実装内容:
- `endAt` 基準で残り時間を計算
- pause/resume時の時間整合性維持
- `COMPLETE` 単発発火ガード

完了条件:
- 0秒到達時に1回だけ完了イベントが発生する
- タイマー計算のユニットテストが通る

### フェーズ3: state-machine導入（1〜1.5日）

目的:
- 状態遷移を安定化する

実装内容:
- 状態: `idle`, `focus`, `short_break`, `long_break`, `paused`
- イベント: `START_FOCUS`, `PAUSE`, `RESUME`, `SKIP`, `COMPLETE`, `RESET`
- `longBreakInterval` ルールの導入

完了条件:
- 遷移表どおりに動作する
- 状態遷移ユニットテストが通る

### フェーズ4: main.js統合（1日）

目的:
- engine + state-machine + DOM を接続する

実装内容:
- ボタン操作とイベントディスパッチ
- 状態に応じたボタン活性制御
- エラーメッセージ表示の基礎

完了条件:
- 開始・停止・再開・リセット・スキップが一連で動作する
- 手動確認シナリオを全通過する

### フェーズ5: 設定機能（localStorage先行）（1日）

目的:
- API実装前に設定UXを完成させる

実装内容:
- `focusMinutes` / `shortBreakMinutes` / `longBreakMinutes` / `longBreakInterval` の設定フォーム
- 入力バリデーション
- `storage.js` で保存・復元

完了条件:
- 再読み込み後に設定が復元される
- 不正値が保存されない

### フェーズ6: 設定API（Flask）（1〜1.5日）

目的:
- フロント保存からAPI保存へ移行可能にする

実装内容:
- `GET /api/settings`, `PUT /api/settings`
- 入力検証とエラーレスポンス形式の統一
- `api-client.js` で通信抽象化

完了条件:
- APIの正常系/異常系テストが通る
- フロントがAPI経由で設定を扱える

### フェーズ7: セッション履歴と統計（2日）

目的:
- MVP後拡張の中核機能を実装する

実装内容:
- `POST /api/sessions/start`
- `POST /api/sessions/complete`
- `GET /api/sessions?date=YYYY-MM-DD`
- `GET /api/stats/today`
- 当日サマリ表示（完了数・集中時間など）

完了条件:
- 集中完了数と合計時間が整合する
- API契約テストが通る

### フェーズ8: Repository抽象とSQLite移行（2日）

目的:
- 将来拡張に備えて永続化を差し替え可能にする

実装内容:
- Repositoryインターフェース定義
- In-memory実装（テスト用）
- SQLite実装（本番用）

完了条件:
- 保存先差し替えでも既存テストが通る
- Flask Controllerを薄く維持できる

### フェーズ9: 通知・仕上げ（1日）

目的:
- 使い勝手を高め、リリース準備を整える

実装内容:
- ブラウザ通知 / 音通知
- 例外時メッセージ整理
- README更新

完了条件:
- 主要ユースケースの受け入れ確認が完了する
- 回帰テストが通る

## 実務上の分割単位（推奨）

1フェーズ内を以下のチケットに分割すると、レビューと不具合切り分けがしやすい。

- チケットA: 純粋ロジックのみ（timer-engine / state-machine）
- チケットB: UI接続のみ（main.js / templates）
- チケットC: 永続化・通信のみ（storage.js / api-client.js / API）
- チケットD: テスト追加のみ

## 目安スケジュール

- 週1: フェーズ0〜4完了（タイマーの基本利用が可能）
- 週2: フェーズ5〜6完了（設定の永続化とAPI化）
- 週3以降: フェーズ7〜9（履歴・統計・SQLite移行・通知）
